
export const remote_base_url = 'https://school-enquiry-backend.onrender.com'   ;

export const local_base_url = 'http://localhost:4000'  ;

// const base_url = process.env.NODE_ENV === 'development' ? local_base_url : remote_base_url  ;